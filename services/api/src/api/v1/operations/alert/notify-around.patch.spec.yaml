# description: 
x-security: 
  - auth: ["user"]
requestBody:
  required: true
  content:
    application/json:
      schema:
        type: object
        properties:
          alertId:
            type: integer
        required:
          - alertId
responses:
  200:
    # description: 
    content:
      application/json:
        schema:
          type: object
          properties:
            ok:
              type: boolean
